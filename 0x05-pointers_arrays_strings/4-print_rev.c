#include "main.h"


/**
 * _puts - prints a string followed by a new line.
 * @str: is of type char
 *
 */

void _puts(char *str)
{
	int n = 0;
	int start;
	while (*str )
{
	n++;
	str++;
}
	s--;
	start = n;
	while (start > 0)
{
	
	_putchar(*s);
	s--;
}
	_putchar('\n');
}
