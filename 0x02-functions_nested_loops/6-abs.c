#include "main.h"

/**
 * _abs - prints the sign of a number.
 * @n: is int
 *
 * Return: 1 if alpha else 0
 */

int _abs(int n)
{
	if (n < 0)
{
		n = n * -1;
		return (n);
}
	else
{
		return (n);
}
	return (0);
}
