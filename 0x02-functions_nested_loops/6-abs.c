#include "main.h"

/**
 * int_abs - prints the sign of a number.
 * @n: is int
 *
 * Return: 1 if alpha else 0
 */

int _abs(int n)
{
	if (n < 0)
{
		c = c * -1;
		return (c);
}
	else
{
		return (c);
}
	return (0);
}
