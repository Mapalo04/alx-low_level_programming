#include "main.h"

/**
 * print_sign - prints the sign of a number.
 * @n: is int
 *
 * Return: 1 if alpha else 0
 */

int print_sign(int n)
{
	if (c > 0)
{
		putchar('+');
		return (1);
}
	else if (c == 0)
{
		putchar('0');
		return (0);
}
	else if (c < 0)
{
		putchar('-');
		return (-1);
}
}

